import 'package:flutter/material.dart';

class TryScreen extends StatelessWidget {
  const TryScreen({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Container(
        child: Text("Welcome to  try screen"),
      ),
    );
  }
}
